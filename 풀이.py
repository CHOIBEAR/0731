# 📘 Pandas 복습 퀴즈 풀이 ✏️  
# ──────────────────────────────────────────────

# Q1. ①  
# → `.loc[]` 슬라이싱은 **끝 라벨까지 포함**됨

# Q2. ④  
# → arr은 2차원 리스트여야 DataFrame 생성 가능

# Q3. ②  
# → 값이 없으면 NaN으로 채워짐

# Q4. ③  
# → 빈 Series와 연산하면 결과는 전부 NaN

# Q5. ②  
# → `sort_values()`는 **열 기준 정렬** 기능

# Q6. ③  
# → 내림차순은 `ascending=False` 설정 필요

# Q7. ③  
# → `.value_counts()`는 값의 **빈도 수** 반환

# Q8. ③  
# → 평균 이상의 값만 필터링하는 조건

# Q9. ①  
# → `index_col=0`은 **첫 번째 컬럼을 인덱스로 사용**

# Q10. ④  
# → `describe()`, `info()`, `head()` 모두 요약에 사용됨

# ──────────────────────────────────────────────
