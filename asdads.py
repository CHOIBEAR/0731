# 📘 Pandas 복습 퀴즈 10문제 🧪  
# ──────────────────────────────────────────────

# ❓ Q1. 다음 중 `.loc["A":"C"]` 슬라이싱의 특징은?

# ① 시작~끝 라벨 포함  
# ② 시작~끝 라벨 미포함  
# ③ 숫자 인덱스를 기준으로 한다  
# ④ 에러가 발생한다  

# ──────────────────────────────────────────────

# ❓ Q2. 다음 중 `pd.DataFrame(arr, idx, col)` 코드에 대한 설명으로 옳은 것은?

# ① arr은 Series여야 한다  
# ② idx는 컬럼 이름이다  
# ③ col은 행 이름이다  
# ④ arr은 2차원 리스트여야 한다  

# ──────────────────────────────────────────────

# ❓ Q3. 서로 다른 DataFrame을 더할 때 값이 존재하지 않으면 어떻게 되는가?

# ① 0으로 처리된다  
# ② NaN이 된다  
# ③ 무시된다  
# ④ 가장 작은 값으로 대체된다  

# ──────────────────────────────────────────────

# ❓ Q4. 빈 Series를 만들고 DataFrame에 더하면 결과는?

# ① 값이 2배로 출력된다  
# ② 원본 값이 유지된다  
# ③ 모두 NaN으로 채워진다  
# ④ 에러가 발생한다  

# ──────────────────────────────────────────────

# ❓ Q5. `sort_values()` 함수의 역할은?

# ① 데이터를 그룹별로 나눈다  
# ② 특정 기준으로 행을 정렬한다  
# ③ 인덱스를 초기화한다  
# ④ 컬럼을 새로 만든다  

# ──────────────────────────────────────────────

# ❓ Q6. 다음 중 영어 점수를 기준으로 내림차순 정렬하려면?

# ① `data.sort_values("영어")`  
# ② `data.sort_values("영어", ascending=True)`  
# ③ `data.sort_values("영어", ascending=False)`  
# ④ `data.sort_values(by="국어")`  

# ──────────────────────────────────────────────

# ❓ Q7. `.value_counts()` 함수의 결과로 올바른 것은?

# ① 컬럼 간 차이를 반환한다  
# ② 인덱스를 제거한다  
# ③ 각 값의 빈도를 세어 반환한다  
# ④ NaN 값을 제거하지 않는다  

# ──────────────────────────────────────────────

# ❓ Q8. 아래 코드의 의미는?  
# `data[data['세대수'] >= data['세대수'].mean()]`

# ① 세대수가 가장 작은 동만 출력  
# ② 평균보다 낮은 세대수만 출력  
# ③ 평균 이상의 세대수인 행만 출력  
# ④ 평균값을 삭제한다  

# ──────────────────────────────────────────────

# ❓ Q9. `pd.read_csv('파일.csv', index_col=0)` 에서 `index_col=0`은 무슨 의미인가?

# ① 첫 번째 컬럼을 인덱스로 사용한다  
# ② 첫 번째 행을 인덱스로 사용한다  
# ③ 첫 번째 컬럼을 삭제한다  
# ④ 컬럼명을 무시한다  

# ──────────────────────────────────────────────

# ❓ Q10. DataFrame의 정보를 요약해서 보여주는 함수는?

# ① `data.describe()`  
# ② `data.info()`  
# ③ `data.head()`  
# ④ 모두 맞다  

# ──────────────────────────────────────────────
